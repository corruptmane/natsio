"""natsio-bench — a fair, reproducible benchmark harness for NATS asyncio clients.

Compares natsio against nats-py and nats-core on core pub/sub, request/reply,
and JetStream (streams, KV, object store) workloads. See :mod:`natsio_bench.cli`.
"""

__version__ = "0.1.0.dev0"
